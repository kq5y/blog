---
export interface Props {
  category: string;
  slug: string;
  title: string;
  tags: string[];
  date: string;
  body: string;
}

const { category, slug, title, tags, date, body } = Astro.props;
---

<article></article>
